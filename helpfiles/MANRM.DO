prm <filename>|<*.*>q
Use to delete a file from the current
directory. The filename must exist 
otherwise an error will be generated.
*.* will delete ALL files in the 
directory, cannot be undone!