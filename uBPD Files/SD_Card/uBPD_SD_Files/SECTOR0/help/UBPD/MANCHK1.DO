chk <src filename> [dest filename]
CHecK the src filename is a valid laptop file format.
  If a dest filename is included a 'fixed' version of the file will be created otherwise only the results will be printed.